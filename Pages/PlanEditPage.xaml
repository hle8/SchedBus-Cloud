<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SchedBus.Pages.PlanEditPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodel="clr-namespace:SchedBus.ViewModels"
    BackgroundColor="LightBlue">

    <ContentPage.BindingContext>
        <viewmodel:PlanEditViewModel />
    </ContentPage.BindingContext>

    <ScrollView>
        <Grid
            Padding="10"
            ColumnSpacing="5"
            RowSpacing="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="25" />
                <RowDefinition Height="80" />
                <RowDefinition Height="30" />
                <RowDefinition Height="80" />
                <RowDefinition Height="80" />
                <RowDefinition Height="50" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="40" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label
                Grid.Row="0"
                Grid.ColumnSpan="3"
                FontAttributes="Bold"
                FontSize="20"
                Text="Destination"
                VerticalTextAlignment="End" />

            <Frame
                Grid.Row="1"
                Grid.ColumnSpan="3"
                BackgroundColor="Transparent"
                BorderColor="Orange"
                CornerRadius="10">
                <Entry Placeholder="Your Destination" Text="" />
            </Frame>

            <Label
                Grid.Row="2"
                Grid.ColumnSpan="3"
                FontAttributes="Bold"
                FontSize="25"
                HorizontalOptions="Center"
                Text="Arrival Time"
                VerticalTextAlignment="End" />

            <CollectionView
                x:Name="TimeSetList"
                ItemsSource="{Binding TimeSet}"
                SelectionMode="Single">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="viewmodel:TimeSetViewModel">
                        <Frame>
                            <Grid ColumnDefinitions="*,*,*">
                                <Label Text="" />
                                <Label Text="{Binding SetTime}" />
                                <Switch IsToggled="{Binding IsEnabled}" />
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <ImageButton
                Grid.Row="5"
                Grid.Column="1"
                Padding="15"
                BackgroundColor="Yellow"
                BorderColor="Orange"
                CornerRadius="10"
                HorizontalOptions="Center"
                Source="plus.png"
                VerticalOptions="Center" />

            <Frame
                Grid.Row="6"
                Grid.ColumnSpan="3"
                BackgroundColor="LightGoldenrodYellow"
                BorderColor="Orange">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Label
                        Grid.Column="0"
                        FontAttributes="Bold"
                        Text="Max Routes"
                        VerticalTextAlignment="Center" />
                    <Entry
                        Grid.Column="1"
                        FontAttributes="Italic"
                        HorizontalTextAlignment="End"
                        Keyboard="Numeric"
                        Placeholder="Number"
                        Text=""
                        VerticalTextAlignment="Center" />
                </Grid>
            </Frame>

            <Frame
                Grid.Row="7"
                Grid.ColumnSpan="3"
                BackgroundColor="LightGoldenrodYellow"
                BorderColor="Orange">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Label
                        FontAttributes="Italic"
                        Text="Vibrartion"
                        VerticalTextAlignment="Center" />
                    <Switch Grid.Column="1" IsToggled="{Binding Vibration}" />
                </Grid>
            </Frame>

            <Frame
                Grid.Row="8"
                Grid.ColumnSpan="3"
                BackgroundColor="LightGoldenrodYellow"
                BorderColor="Orange">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Label
                        Grid.Column="0"
                        FontAttributes="Bold"
                        Text="Label"
                        VerticalTextAlignment="Center" />
                    <Entry
                        Grid.Column="1"
                        FontAttributes="Italic"
                        HorizontalTextAlignment="End"
                        Keyboard="Text"
                        Placeholder="Name"
                        Text="{Binding Label}"
                        VerticalTextAlignment="Center" />
                </Grid>
            </Frame>

            <Button
                Grid.Row="9"
                Grid.Column="0"
                BackgroundColor="LightSlateGrey"
                BorderColor="Orange"
                FontAttributes="Bold"
                FontSize="15"
                Text="Delete"
                TextColor="Black" />

            <Button
                Grid.Row="9"
                Grid.Column="2"
                BackgroundColor="LightGreen"
                BorderColor="Orange"
                FontAttributes="Bold"
                FontSize="15"
                Text="Save"
                TextColor="Black" />

        </Grid>
    </ScrollView>
</ContentPage>